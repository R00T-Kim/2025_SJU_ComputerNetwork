# Simple IRC Chat Project

Python 소켓 프로그래밍을 활용하여 구현한 멀티스레드 기반의 **IRC(Internet Relay Chat)** 서버 및 클라이언트 프로그램입니다.
RFC 1459 프로토콜의 핵심 기능을 준수하여 개발되었으며, GUI 클라이언트를 통해 직관적인 채팅 환경을 제공합니다.

## 🌟 주요 기능 (Features)

### 서버 (Server)
- **멀티스레드 지원**: `threading` 모듈을 사용하여 다수의 클라이언트 접속을 동시에 처리합니다.
- **채널 관리**: 동적인 채널 생성, 입장(JOIN), 퇴장(PART), 사용자 목록 조회(NAMES)를 관리합니다.
- **프로토콜 준수**: RFC 1459 표준에 기반한 메시지 파싱 및 응답 처리를 수행합니다.
- **로깅 시스템**: 서버 상태 및 에러를 콘솔에 기록하여 디버깅을 돕습니다.

### 클라이언트 (Client)
- **GUI 인터페이스**: Python 내장 라이브러리인 `tkinter`를 사용하여 사용자 친화적인 GUI를 제공합니다.
- **편의성 기능**: 
    - 서버 접속 정보(IP, Port) 및 닉네임 설정
    - 명령어 단축키 지원 (예: `/join #chat`)
    - 실시간 채팅 로그 표시 및 자동 스크롤
- **자동 응답**: 서버의 `PING` 요청에 대해 자동으로 `PONG` 응답을 보내 연결을 유지합니다.

## 🛠 기술 스택
- **Language**: Python 3.8+
- **Network**: TCP/IP Socket (`socket` module)
- **GUI**: Tkinter
- **Concurrency**: Threading

## 🚀 실행 방법 (How to Run)

**주의사항**: 반드시 프로젝트 최상위 디렉토리(`2025_SJU_ComputerNetwork`)에서 명령어를 실행해야 합니다.

### 1. 서버 실행
```powershell
# 프로젝트 루트 경로에서 실행
python -m src.server
```
- 서버는 기본적으로 `0.0.0.0:6667` 포트에서 대기합니다.

### 2. 클라이언트 실행
새로운 터미널 창을 열고 실행하세요.
```powershell
# 프로젝트 루트 경로에서 실행
python -m src.client
```
- 클라이언트 창이 뜨면 Host, Port, Nickname을 입력하고 **Connect** 버튼을 누릅니다.
- 기본값: Host(`127.0.0.1`), Port(`6667`)

## 💬 사용 가능한 명령어

클라이언트 채팅 입력창에서 아래의 명령어들을 사용할 수 있습니다.

| 명령어 | 사용법 | 설명 |
| :--- | :--- | :--- |
| **JOIN** | `/join #채널명` | 특정 채널에 입장합니다. (예: `/join #general`) |
| **PART** | `/part #채널명` | 현재 채널에서 퇴장합니다. |
| **NICK** | `/nick 새닉네임` | 닉네임을 변경합니다. |
| **MSG** | 일반 텍스트 입력 | 현재 접속 중인 채널로 메시지를 전송합니다. |

## 📂 프로젝트 구조

```bash
2025_SJU_ComputerNetwork/
├── docs/                      # 프로젝트 문서
│   ├── RFC1459_Summary.md     # 프로토콜 분석 내용
│   └── Team_Info.txt          # 팀원 정보
├── src/                       # 소스 코드
│   ├── __init__.py
│   ├── server.py              # [Entry] 서버 메인 실행 파일
│   ├── client.py              # [Entry] 클라이언트 메인 실행 파일 (GUI)
│   ├── core/                  # 핵심 로직 모듈
│   │   ├── handler.py         # 클라이언트 요청 처리 핸들러
│   │   ├── channel_manager.py # 채널 및 유저 상태 관리
│   │   └── parser.py          # IRC 메시지 파서
│   └── utils/                 # 유틸리티
│       └── logger.py          # 로거 설정
├── requirements.txt           # 의존성 목록
└── README.md                  # 프로젝트 설명서
```

## 📝 참고 사항
- 이 프로젝트는 세종대학교 컴퓨터네트워크 팀 프로젝트 과제입니다.
- 자세한 프로토콜 명세는 `docs/RFC1459_Summary.md`를 참고하세요.
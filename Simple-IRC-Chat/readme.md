# Simple IRC Chat

Python 소켓 프로그래밍을 이용한 간단한 IRC(Internet Relay Chat) 채팅 프로그램입니다.
RFC 1459 프로토콜의 일부 핵심 기능을 구현하는 것을 목표로 합니다.

## 📂 프로젝트 구조

```
Simple-IRC-Chat/
├── docs/                      # 문서 관련
│   ├── Team_Info.txt          # 팀원 정보 (제출용)
│   └── RFC1459_Summary.md     # 구현 프로토콜 요약
├── src/                       # 소스 코드
│   ├── server.py              # [메인] 서버 실행 파일
│   ├── client.py              # [메인] 클라이언트 실행 파일 (CLI)
│   │
│   ├── core/                  # 서버 핵심 로직
│   │   ├── handler.py         # 클라이언트 스레드 핸들러
│   │   ├── channel_manager.py # 채널 및 유저 관리
│   │   └── parser.py          # 명령어 파싱
│   │
│   └── utils/                 # 유틸리티
│       └── logger.py          # 로그 출력
└── readme.md                  # 프로젝트 설명서
```

## 🚀 실행 방법

### 1. 서버 실행
터미널을 열고 `src` 폴더 내의 `server.py`를 실행합니다.
```bash
cd Simple-IRC-Chat/src
python server.py
```
서버는 기본적으로 `127.0.0.1:6667`에서 수신 대기합니다.

### 2. 클라이언트 실행
새로운 터미널을 열고 `src` 폴더 내의 `client.py`를 실행합니다.
```bash
cd Simple-IRC-Chat/src
python client.py
```

## 💡 주요 명령어 (예정)
- `NICK <닉네임>`: 닉네임 설정
- `JOIN <#채널명>`: 채널 입장
- `PRIVMSG <대상> <메시지>`: 메시지 전송
- `QUIT`: 접속 종료

## 🎨 GUI 확장 (Option)
현재 `client.py`는 CLI 기반이나, 추후 `tkinter` 등을 활용하여 GUI 클라이언트로 확장 가능하도록 구조가 잡혀 있습니다.
